package di

// WebhookBusKey is the DI container key for the webhook bus provider
const WebhookBusKey = "WebhookBus"

// WebhookDeliveryKey is the DI container key for the webhook delivery handler
const WebhookDeliveryKey = "WebhookDelivery"

// RetryWorkerKey is the DI container key for the webhook retry worker
const RetryWorkerKey = "WebhookRetryWorker"
